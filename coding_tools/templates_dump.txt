=== TEMPLATE DUMP ===

----------------------------------------
FILE: coding_tools/ai_prompt_templates/codex_chat_regeln.txt
----------------------------------------
# Zweck
# - Definiert dauerhafte Arbeitsregeln für Codex innerhalb dieses Projekts.
# - Wird zu Beginn eines neuen Codex-Chats geladen.
# - Gilt für alle folgenden Runs.

Projekt
Kino-App (Tagesabschluss)
Flutter / Dart
Repository: kino_bar_app
Branch: master

Grundprinzip
Dieses Projekt arbeitet mit kleinen, kontrollierten Entwicklungs-Runs.

Arbeitsweise
- Änderungen erfolgen ausschließlich über explizite Codex-Run-Prompts.
- Ein Run hat immer genau einen klaren Fokus.
- Keine Nebenbei-Refactors.
- Keine Architekturänderungen ohne Architektur-Run.

Ohne Run-Prompt NICHT erlaubt
- Code ändern
- Dateien umbenennen oder verschieben
- neue Packages hinzufügen
- pubspec ändern
- Plattform- oder Build-Konfiguration ändern
- großflächige Formatierungsänderungen

Ohne Run-Prompt erlaubt
- Verständnisfragen stellen
- kurze Analyse des Codes
- Rückfragen bei unklaren Anforderungen

Technische Leitplanken
- Interne Berechnung weiterhin in Cent.
- Persistenz-Keys dürfen nicht verändert werden.
- UI darf nur im Zielbereich eines Runs geändert werden.
- Keine neuen Dependencies ohne Freigabe.

Run-Regel
Ein Run-Prompt enthält immer:
- Kontext
- Ziel
- Akzeptanzkriterien
- technische Regeln
- Git-Anweisungen

Wenn ein Prompt unklar ist:
→ Rückfragen stellen, keine Änderungen durchführen.

LOCK (WICHTIG)
- Führe keine Änderungen aus.
- Erstelle keinen Code.
- Warte auf den nächsten Codex Run Prompt.

Antwortformat auf diesen Prompt:
Antworte ausschließlich mit: "Bereit. Warte auf Run-Prompt."
----------------------------------------
FILE: coding_tools/ai_prompt_templates/codex_run_architektur_struktur.txt
----------------------------------------
# Zweck:
# - Template für Struktur-/Architekturarbeiten (z.B. Usecase-Layer, Entkoppelung UI/Storage, Ordnerstruktur innerhalb lib/).
# - Wird genutzt, wenn wir bewusst Schichten trennen oder technische Schuld reduzieren.
# - Bestimmt: enge Grenzen, damit nichts „nebenbei“ umgebaut wird.

Codex Run <NUMMER>: <Kurzbeschreibung Architektur/Struktur>

Kontext:
- Projekt: Kino-App (Tagesabschluss)
- Branch: master
- Warum ist diese Strukturänderung nötig? (1–3 Sätze)
- Betroffene Dateien/Ordner: <Liste>
- Was ist aktuell problematisch? <kurz>

Ziel:
- Was wird ausgelagert / neu eingeführt / getrennt?
- Was bleibt unverändert (UI, Keys, Modelle)?

Verbindliche Regeln:
1) Keine UI-Änderungen (nur Verkabelung/Calls).
2) Keine Key-/JSON-Änderungen.
3) Keine fachliche Logik neu erfinden; nur verschieben/kapseln.
4) Neue Dateien nur, wenn unbedingt nötig – und dann minimal.

Technische Vorgaben:
- Clean-Arch-ähnlich, aber pragmatisch: Usecase/Service als dünne Schicht zwischen UI und Storage.
- Keine neuen Packages.
- Bestehende Benennungen beibehalten.

Erwartetes Ergebnis:
- Welche Schicht bekommt welche Verantwortung?
- Liste der geänderten/neu angelegten Dateien.

Abschluss:
- flutter analyze muss sauber sein.
- flutter test muss grün sein (falls Tests vorhanden).
- Liste der geänderten Dateien.

GIT (verbindlich – von dir auszuführen):
Sicherheitsregel wie im Standard-Template.
Reihenfolge:
1) git status
2) git add <nur betroffene Dateien>
3) git commit -m "Run <NUMMER>: <Kurzbeschreibung>"
4) git status
5) git push
----------------------------------------
FILE: coding_tools/ai_prompt_templates/codex_run_dokumentation.txt
----------------------------------------
# Zweck:
# - Kommentar-/Dokumentationsarbeiten ohne Code-Verhalten zu ändern.
# - Wird genutzt für: Code-Kommentare, kurze README-Abschnitte, Hinweise für spätere Wartung.
# - Bestimmt: "Kommentar only" – keinerlei Refactor/Logik/UI.

Codex Run <NUMMER>: <Kurzbeschreibung Doku/Kommentare>

Kontext:
- Projekt: Kino-App (Tagesabschluss)
- Branch: master
- Welcher Bereich soll besser verständlich werden? <Dateien/Ordner>
- Was ist aktuell unklar? <kurz>

Aufgabe:
- Ergänze/verbessere Kommentare in <Zielbereich>.

Verbindliche Regeln:
- KEINE Logikänderungen.
- KEINE UI-Änderungen.
- KEINE Refactorings.
- KEINE neuen Funktionen.
- KEINE neuen Dateien (außer ausdrücklich gefordert).
- Sprache: Deutsch, kurz, sachlich.

Abschluss:
- Liste der Dateien, in denen Kommentare ergänzt wurden.

GIT (verbindlich – von dir auszuführen):
Sicherheitsregel wie im Standard-Template.
Reihenfolge:
1) git status
2) git add <nur betroffene Dateien>
3) git commit -m "Run <NUMMER>: <Kurzbeschreibung>"
4) git status
5) git push
----------------------------------------
FILE: coding_tools/ai_prompt_templates/codex_run_standard.txt
----------------------------------------
# Zweck:
# - Standard-Template für normale Änderungen (UX, Logik, kleine Refactors im Zielbereich).
# - Wird für 80–90% der Codex-Runs verwendet.
# - Bestimmt: Kontext, Ziel, Regeln, Nicht-Erlaubtes, erwartetes Ergebnis und Git-Commit (von Codex auszuführen).

Codex Run <NUMMER>: <Kurzbeschreibung>

WICHTIG (Run-Nummer):
- Die Run-Nummer MUSS der Zeile "Nächster Run:" aus coding_tools/ai_prompt_templates/run_counter.txt entsprechen.
- Wenn das nicht passt: STOPP und Rückfrage.

Kontext:
- Projekt: Kino-App (Tagesabschluss)
- Repository: kino_bar_app
- Branch: master
- Aktueller Stand in 3–6 Stichpunkten.
- Betroffene Screens/Dateien: <Dateien/Ordner>
- Was existiert bereits? <kurz>
- Was darf NICHT neu gedacht werden? <kurz>

Ziel:
- In 1–3 Sätzen: Was ist nachher konkret anders?

Akzeptanzkriterien (Test in Simulator/Device):
1) <Kriterium 1>
2) <Kriterium 2>
3) <Kriterium 3>

Verbindliche fachliche Regeln:
1) <Regel 1>
2) <Regel 2>

Technische Vorgaben:
- Interne Berechnung weiterhin in Cent.
- Keine neuen Packages (außer ausdrücklich gefordert).
- Keine Umbenennung von Dateien/Klassen ohne ausdrückliche Anweisung.
- Keine Änderung an Persistenz-Keys/JSON-Struktur ohne ausdrückliche Anweisung.
- Änderungen strikt auf den beschriebenen Zielbereich begrenzen.

Nicht erlaubt:
- Kein Refactoring außerhalb des Zielbereichs.
- Keine UI-Umgestaltung außerhalb des Zielbereichs.
- Keine „Nebenbei“-Änderungen an pubspec.*, iOS/Android/macOS-Konfig, Pods etc. (außer ausdrücklich gefordert).

Kommentierpflicht (Code):
- Neue/angepasste Klassen und fachlich relevante Methoden kurz und sachlich auf Deutsch kommentieren („Was macht das?“ ggf. „Warum so?“).
- Keine Kommentare zu trivialem Flutter-Standardcode.

Erwartetes Ergebnis:
- Kurz beschreiben, wie es nachher aussieht.
- Liste der voraussichtlich angepassten Dateien.

Abschluss (deine Antwort):
- Liste der tatsächlich geänderten Dateien.
- Kurzer Hinweis, wie ich testen kann (3 Bulletpoints).
- flutter analyze muss sauber sein.
- flutter test muss grün sein (falls Tests vorhanden).

GIT (verbindlich – von dir auszuführen):
- Führe die Git-Befehle selbst aus (nicht nur vorschlagen).
- Commit-Message exakt: "Run <NUMMER>: <Kurzbeschreibung>" (Deutsch).
- Sicherheitsregel: Wenn git status NICHT "On branch master" zeigt ODER wenn „detached HEAD“ / unerwartete Deletes / fremde Änderungen auftauchen:
  - STOPPE und gib NUR eine kurze Diagnose + sichere, nicht-destruktive Schritte aus (keine automatischen clean/restore ohne ausdrückliche Anweisung).

Reihenfolge:
1) git status
2) git add <nur betroffene Dateien>   (kein -A, außer ich fordere es explizit)
3) git commit -m "Run <NUMMER>: <Kurzbeschreibung>"
4) git status
5) git push

Nach erfolgreichem Commit:

1. Öffne coding_tools/ai_prompt_templates/run_counter.txt
2. Erhöhe:
   Letzter abgeschlossener Run → aktuelle Run-Nummer
   Nächster Run → aktuelle Run-Nummer +1
3. Commit:

git add coding_tools/ai_prompt_templates/run_counter.txt
git commit -m "Update run counter after Run <NUMMER>"
git push
----------------------------------------
FILE: coding_tools/ai_prompt_templates/entwicklungsabschnitt_start.txt
----------------------------------------
# Zweck:
# - "Arbeitsabschnitt starten": kurzer Reset, damit wir wieder in den Flow kommen.
# - Wird am Anfang eines neuen Chats oder nach einer Pause verwendet.
# - Bestimmt: aktueller Stand, offene Punkte, nächster Mini-Schritt, Run-Nummer.

Entwicklungsabschnitt – Start

Projekt:
- Kino-App (Tagesabschluss), NICHT BudzBunny.
- Repository: kino_bar_app
- Branch: master

Git-Stand (optional, aber empfohlen):
- Letzter Commit: <git log -1 --oneline>

Aktueller Stand (kurz):
- <3–6 Bulletpoints>

Bekannte offene Probleme:
- <Liste>

Nächster Mini-Schritt:
- <1 konkretes Ziel, klein testbar>

Run-Nummer:
- Quelle: coding_tools/ai_prompt_templates/run_counter.txt
- Nächster Run: <NUMMER>  (muss der Zeile "Nächster Run:" entsprechen)

Arbeitsmodus:
- Kleine Prompts, nach jedem Run testen.
- Nach bestandenem Test: Commit + Push durch Codex gemäß Template.

Startfrage an mich:
- “Bist du bereit für Run <NUMMER>?”

Antwort:
- go
----------------------------------------
FILE: coding_tools/ai_prompt_templates/kino_app_zusammenarbeitsregeln.md
----------------------------------------
# Zusammenarbeit & Run-Regeln – Kino-App Projekt

## 1. Projektkontext
- Projekt: Kino-App (Tagesabschluss)
- Branch: master
- Kleine, kontrollierte Runs
- Keine Nebenbei-Refactors

---

## 2. Chat-Start-Regel
Neuer Chat beginnt immer mit dem Block aus `entwicklungsabschnitt_start.txt`.
Ohne diesen Block wird kein Run gestartet.

---

## 3. Run-Typen

### Standard-Run
Vorlage: `codex_run_standard.txt`
Für UX-/Logikänderungen im klar definierten Zielbereich.

### Architektur-Run
Vorlage: `codex_run_architektur_struktur.txt`
Für Struktur-/Schichtentrennung ohne UI-Redesign.

### Doku-Run
Vorlage: `codex_run_dokumentation.txt`
Nur Kommentare. Keine Logikänderungen.

---

## 4. Run-Disziplin
- Maximal ein klarer Mini-Fokus pro Run
- Testbar in unter 2 Minuten
- Keine Misch-Runs
- Run-Nummern fortlaufend

---

## 5. Git-Regeln (verbindlich)
Die verbindlichen Git-Regeln (Sicherheitsregel, Add-Regel, Commit-Format, Reihenfolge) stehen in:
- `codex_run_standard.txt`

Für Runs ist dieses Template die „Single Source of Truth“.

---

## 6. Test-Regel
Nach jedem Run:
- 3 klare Testschritte
- Erwartetes Verhalten definiert
- Erst nach bestandenem Test → Commit

### Ergänzung zu §6 (Run-Bericht)
Jeder Run-Bericht enthält zusätzlich:
- **Empfohlene Test-Aktionen** (mind. 3)
- **Erwartetes Verhalten** pro Test-Aktion (kurz und prüfbar)

ChatGPT weist aktiv darauf hin, wenn ein neuer Codex-Chat empfohlen wird (z. B. bei Kontext-Drift oder Themenwechsel).

---

## 7. Prompt-Regel für Codex
- Prompts stehen immer in kopierbaren Codeblöcken
- Git-Anweisung ist im Prompt enthalten
- Keine verkürzten Anweisungen
- Keine impliziten Annahmen

---

## 8. Technische Leitplanken
- Interne Berechnung in Cent
- Keine neuen Packages ohne Freigabe
- Keine Key-/Persistenz-Änderungen ohne Freigabe
- Keine UI-Umgestaltung außerhalb des Zielbereichs
- Keine pubspec-/Pod-/Build-Änderungen nebenbei

---

## 9. Arbeitsphilosophie
- Kleine Schritte
- Klare Verantwortung
- Stabilität vor Geschwindigkeit
- Kein „Nebenbei“

---

## 10. Neuer Chat wenn:
- Git-Historie verwirrend wird
- Run-Nummern unsauber wirken
- Mehrere Themen vermischt wurden

---

## 11. Run-Erstellung (verbindlicher Ablauf)
1. ChatGPT erstellt zunächst nur einen Run-Vorschlag (ohne Codeblock).
2. User bestätigt mit "go".
3. Erst danach wird ein einziger finaler Codex-Prompt als Copy-Block erzeugt.
4. Es darf nie mehr als ein aktiver Prompt mit derselben Run-Nummer existieren.
5. Wird ein Prompt angepasst, erhält er eine neue Run-Nummer.

---

## 12. Codex-Chat-Wechsel

Ein neuer Codex-Chat wird **nicht automatisch bei jedem Run** gestartet.

Ein neuer Chat ist empfohlen, wenn:
- ein klarer Kontextwechsel erfolgt (z. B. UI → Architektur, Schritt 1 → neues Feature)
- Git-Komplexität entstanden ist (Merge, Rebase, Reset, Detached HEAD, unerwartete Changes)
- mehrere Prompt-Varianten mit derselben Run-Nummer existieren
- Codex inkonsistent wirkt oder alte Annahmen weiterverwendet
- mehr als ca. 5–8 zusammenhängende Runs ohne Themenwechsel erfolgt sind

### Ablauf bei Chat-Wechsel
1. Letzten Run sauber committen und pushen.
2. Neuen Chat mit `entwicklungsabschnitt_start.txt` beginnen.
3. Run-Nummer fortlaufend weiterführen (nicht zurücksetzen).

ChatGPT weist aktiv darauf hin, wenn ein neuer Codex-Chat empfohlen wird.
----------------------------------------
FILE: coding_tools/ai_prompt_templates/run_counter.txt
----------------------------------------
RUN COUNTER – Kino-App

Letzter abgeschlossener Run: Run 58
Nächster Run: Run 59

