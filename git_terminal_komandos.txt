Aktuellen Commit (HEAD) anzeigen:
  git log -1

Nur die Commit-Message des aktuellen Commits:
  git log -1 --pretty=%B

Kurzform (Hash + Message in einer Zeile):
  git log -1 --oneline

Nur den Commit-Hash:
  git rev-parse HEAD