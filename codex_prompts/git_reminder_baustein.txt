// folgenden Prompt am ende des prompts einfügen, damit Codex den User 
// daran erinnert, den Commit-Befehl auszuführen

WICHTIG – GIT-REMINDER:
Am Ende deiner Antwort MUSST du mich explizit daran erinnern,
jetzt einen Git-Commit zu machen.

Gib mir:
1) den passenden Commit-Befehl
2) eine konkrete Commit-Message
3) den Hinweis, vorher git status zu prüfen.

nach folgendem Muster:
git status
git add lib/domain/tagesabschluss_berechnung.dart lib/pages/tagesabschluss_schritt1_seite.dart lib/pages/tagesabschluss_schritt2_seite.dart test/widget_test.dart
git commit -m "Refactor: <commit name>"
git status